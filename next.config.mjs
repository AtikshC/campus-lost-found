/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/campus-lost-found",
  assetPrefix: "/campus-lost-found/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
